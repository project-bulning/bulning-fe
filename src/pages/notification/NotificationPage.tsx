import { DefaultPaddedContainer } from '@components/container/variants';

function NotificationPage() {
  return (
    <DefaultPaddedContainer>알림페이지</DefaultPaddedContainer>
  );
}

export default NotificationPage;
